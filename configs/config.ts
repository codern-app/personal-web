const siteName = process.env.NEXT_PUBLIC_SITE_NAME;
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL;
const userTitle = process.env.NEXT_PUBLIC_USER_TITLE;
const userDescription = process.env.NEXT_PUBLIC_USER_DESCRIPTION;
const userImageUrl = process.env.NEXT_PUBLIC_USER_IMAGE_URL;

export const config = {
  siteName,
  siteUrl,
  userTitle,
  userDescription,
  userImageUrl,
};
